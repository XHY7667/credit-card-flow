spring.datasource.url=jdbc:postgresql://localhost:5432/${POSTGRES_DB:creditcardflow}
spring.datasource.username=${POSTGRES_USER:creditcardflow}
spring.datasource.password=${POSTGRES_PASSWORD}
spring.jpa.hibernate.ddl-auto=none
spring.flyway.enabled=false
